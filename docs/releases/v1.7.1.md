# JustRayzist v1.7.1

- Fixes fullscreen prompt details so wildcard substitutions appear only when the prompt is expanded, below the final prompt text, and are excluded from prompt copy actions.
- Adds wildcard tile double-click insertion: single click still copies the placeholder, while double click copies once and appends the placeholder to the end of the prompt.
- Restores the flat accent treatment on the library headers so LoRA stays magenta and Wildcard stays green.

<p align="center">
  <img width="900" alt="Wildcard library preview" src="../../readme_images/wildcards_preview.png" />
</p>
