# JustRayzist v1.8.4

- Fixes extracted Z-Image LoRAs whose additive `diff_b` tensors target modules that PEFT rewrites behind `base_layer` wrappers after LoRA adapter load.
- Resolves extracted direct-delta targets through wrapper aliases such as `base_model.model.*`, `module.*`, `_orig_mod.*`, and `*.base_layer.*` so stacked adapters still land on the intended transformer tensors.
- Adds regression coverage for wrapped `cap_embedder`, `all_x_embedder`, and `all_final_layer` direct-delta application and revert behavior.
