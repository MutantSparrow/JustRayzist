# JustRayzist v1.8.2

- Retunes the shipped default `upscale` path after `v1.8.1` oversharpen regression reports while keeping the same `baseline_ai_x2 + FS` architecture.
- Lowers the default upscale photo img2img similarity from `0.90` to `0.85` and cuts the upscale-only FS intensity from `8` to `4`.
- Leaves the `v1.8.1` default `clarity` pipeline unchanged, adds regression coverage for the new upscale-only knobs, and refreshes bootstrap packages for both CUDA lanes.
