# JustRayzist v1.8.3

- Fixes LoRA and Wildcard drawer masonry relayout jumps so library scrolling stays anchored while cards reflow.
- Reworks default `/upscale` into a content-aware x2 path that auto routes photos to the detail stack and illustrations to an art-preserve stack.
- Reduces tiled upscale seam drift by defaulting to the current-baseline SeedVR2 runtime policy first, widening decode overlap on tiled fallback, and adding source-guided seam repair telemetry.
