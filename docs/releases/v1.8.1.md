# JustRayzist v1.8.1

- Promotes `baseline_ai_x2 + FS` to the shipped default upscale path, replacing the older plain SeedVR2 single-pass default.
- Rebuilds default `clarity` around the photo fidelity stack: x2 structural cleanup, multiband detail transfer, original chroma restore, edge-aware sharpening, FS sharpen, final unsharp, and downscale back to source size.
- Aligns release docs, API examples, and web UI helper text with the new upscale and clarity behavior so packaged builds describe the actual shipped pipelines.
- Cleans out engineering-only benchmark leftovers before release packaging so the shipped tree stays focused on runtime behavior and release assets.
