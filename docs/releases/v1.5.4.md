# JustRayzist v1.5.4

![LoRA library preview](../../readme_images/lora_preview.png)

- Restores the LoRA drawer masonry layout using explicit JS-managed columns instead of CSS multi-column flow, so the drawer no longer shows clipped off-screen overflow columns.
- Keeps the passive-browser LoRA sync fixes in place, including the event-stream refresh path and no-cache handling for list/preview responses.
- Preserves LoRA card hover, focus, and title fallback behavior while rebalancing the drawer after preview-image loads and window resizes.
